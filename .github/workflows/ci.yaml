---

name: CI

# yamllint disable-line rule:truthy

on:
push:
pull_request:
types:
- opened
- reopened
- synchronize
workflow_dispatch:

jobs:
workflows:
permissions:
contents: read
packages: write
attestations: write
id-token: write
uses: jhollowe-addons/workflows/.github/workflows/addon-ci.yaml@main
